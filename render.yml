services:
  - type: web
    name: masesora-backend
    env: python
    region: frankfurt
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: sh -c "exec uvicorn main:app --host 0.0.0.0 --port $PORT"
    pythonVersion: 3.10
