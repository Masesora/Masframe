services:
  - type: web
    name: masesora-backend
    env: python
    region: frankfurt
    plan: starter
    rootDir: masesora_backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    pythonVersion: 3.10