services:
  - type: web
    name: masframe-backend
    env: python
    rootDir: masesora_backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"